apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "google-tag-manager.fullname" . }}-test-connection"
  labels:
    {{- include "google-tag-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "google-tag-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
